# Project
Mini project demonstrating:
- Git workflow (feature branches + merges)
- Linux basics (users, permissions, ssh keys, services)
- Bash automation scripts
- Cron scheduling plan

# Logs
- Run log archiver: `bash scripts/log_archive.sh`
- Run audit report: `bash scripts/user_audit.sh`
